# Linux Packaging

Phase 4 uses CPack to generate Linux DEB, TGZ, and ZIP artifacts. Package metadata and install rules live in `cmake/Packaging.cmake`.

This directory is reserved for future Linux-specific assets such as desktop entries, icons, maintainer scripts, and distribution policy files.
